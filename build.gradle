buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // إصدار متوافق ومستقر لـ Android Gradle Plugin
        classpath 'com.android.tools.build:gradle:7.4.2'
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
